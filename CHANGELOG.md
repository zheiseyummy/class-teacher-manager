# Changelog

## Unreleased

- 清理早期网页原型、阶段性截图、一次性 UI 审计报告和历史构建余留。
- 重写完整 README，统一安装、数据、Excel、测试、打包和后续开发说明。
- 将正式考勤使用的 `LeaveRecord` 独立到 `models/attendance.py`，移除未使用且已经取消的预留模型。
- 将视觉 QA 输出统一迁移到被 Git 忽略的 `build/visual-qa/`。

## v1.0.1 - 2026-08-30

正式版兼容性修复。

- 隔离打包环境的 DLL 搜索路径，避免误收集 Poppler 的 ICU 运行库，修复导入 `QtCore` 时提示“找不到指定的程序”。
- 启动失败时显示明确的系统与解压要求，并在程序目录生成 `启动诊断.txt`。
- 在便携包中加入安装与启动说明，明确系统版本和完整解压要求。

## v1.0.0 - 2026-08-30

首个正式版本。

- 完成产品级 PySide6 校园管理工作台 UI。
- 完成桌面侧栏与 390px 紧凑窗口导航。
- 统一学生、综合素质、成绩、德育、考勤、课程表、日历和备份模块的工具栏、表格、详情与状态样式。
- 增加空数据、无搜索结果、禁用、焦点、选中和危险操作视觉状态。
- 增加关键表单和最终综合素质复核窗口的响应式尺寸与滚动支持。
- 保留删除、恢复和最终结果锁定的人工确认流程。
- 增加正式应用图标、Windows EXE 文件版本信息和版本化便携 ZIP 构建。
- 13 项现有冒烟测试、原生 Windows 截图 QA、编译与依赖检查通过。
