# 班主任综合管理系统：项目进度与下一步

> 更新时间：2026-07-23
> 用途：下次继续开发前先阅读本文件，用它核对当前状态、已确认规则和下一步优先级。
> 当前原则：先确认网页交互原型，再分模块迁移到正式 PySide6 程序；除非明确提出，否则每次修改后不打包 EXE。
> Git 基线：`main` 分支，标签 `baseline-2026-07-23`。下次改动前可用该标签核对或恢复当前版本。

## 0. 一分钟结论

目前存在两套需要明确区分的内容：

1. **正式桌面程序**位于项目根目录，以 `main.py` 启动，使用 PySide6、SQLAlchemy 和 SQLite，已经能够真实保存和处理数据。
2. **新版 UI 网页原型**位于 `ui-prototype/`，用于确认蓝色侧栏、办公工作台、模块布局和交互方向。目前使用模拟数据，尚未连接正式 SQLite 数据库，也尚未迁回 PySide6。

下一条主线是：

`确认网页原型 -> 提炼通用视觉规范 -> 迁移正式程序外壳与工作台 -> 优先升级成绩管理 -> 迁移综合素质 -> 逐步统一其余模块 -> 最终验收后按需打包 EXE`

## 1. 项目当前形态

| 项目 | 位置 | 当前用途 | 数据性质 |
| --- | --- | --- | --- |
| 正式 Windows 桌面程序 | `main.py`、`views/`、`controllers/`、`models/` | 日常功能开发和真实使用 | 真实 SQLite 数据 |
| 新版网页交互原型 | `ui-prototype/` | 验证视觉、布局和操作方式 | 模拟数据，不写正式数据库 |
| 当前源码数据库 | `data/class_manager.db` | 源码运行时的数据文件 | 本地真实数据 |
| 默认备份目录 | `backups/` | ZIP 数据备份 | 本地备份 |
| 最新已有 EXE | `dist-python/ClassTeacherManager/ClassTeacherManager.exe` | 当前已存在的打包版本 | 2026-07-23 生成 |

项目中还保留 `dist-python-updated/`、`dist-python-workbench/` 和 `build/` 等历史构建产物。它们不是下一轮开发的源代码依据；待确认不再需要后可以统一清理。当前 `.git` 目录为空，尚未形成可用的 Git 版本历史。

## 2. 已确认的总体要求

- Windows 本地运行，不依赖网络。
- Python + PySide6 桌面界面，SQLAlchemy ORM，SQLite 本地数据库。
- 最终可打包为 EXE，但只在明确要求时打包。
- 支持 Excel 导入和导出；PDF 导出仍是待开发项。
- 支持多个学生班级，也支持一位教师同时教授多个教学班。
- 页面采用左侧导航、顶部搜索和操作区、右侧内容区。
- 模块化开发，每个模块保持可独立测试，并方便继续增加功能。
- 教师办公场景优先：信息密度适中、操作直接、颜色克制、适合长时间使用。
- 不开发家访、家长谈话等模块。
- 综合素质不建立违纪扣分明细，也不根据成绩、体育或活动自动生成过程等级。
- 综合素质中的均衡生户籍、连续就读和资格筛选需求已经取消，对应旧需求 Q18、Q19 不再开发。

## 3. 正式桌面程序已完成内容

### 3.1 基础框架与本地数据

- 已建立 `main.py / database / models / controllers / views / utils / resources / tests` 模块结构。
- 已接入 SQLite 和 SQLAlchemy。
- 源码运行数据保存到 `data/class_manager.db`。
- 打包版数据设计为保存到 `%LOCALAPPDATA%\LocalClassManager\`。
- 已提供启动脚本、打包脚本、使用说明和自动化测试。

### 3.2 学生数据中心

- 多班级管理。
- 学生姓名、性别、学号、年级、班级、座号、出生日期、身份证号、民族和备注。
- 一个学生可关联多个家长联系人。
- 支持姓名、学号、班级和家长电话查询。
- 支持新增、编辑、查看学生档案。
- 支持学生 Excel 模板、批量导入、重复检查和错误结果提示。
- 支持全部学生、单班学生和家长通讯录 Excel 导出。
- 未提前建档的综合素质或成绩学生可在导入时自动建档。

### 3.3 综合素质评价

正式主流程已经闭环：

`六学期 Excel 导入 -> 九下名单确认 -> 五维累计 -> 五维独立班级排名 -> A/B/C 自动分档 -> 教师调整最终等级 -> 锁定 -> 最终档案导出`

已实现：

- 六个学期：七上、七下、八上、八下、九上、九下。
- 五个维度：思想品德、学业水平、身心健康、艺术素养、实践与创新。
- 新录入只使用 A/B/C，缺失数据使用 N/A；不再出现 D。
- 可导入包含六张学期工作表的 Excel。
- 可不依赖学生中心单独导入，系统自动建立目标班级和学生。
- 缺失学期保持 N/A，只累计已有成绩，不补分、不折算。
- 最终名单以九下实际名单为准；九下前转出的学生保留历史但不进入最终评定。
- 五个维度分别按班级排名，同分同名次，分档不拆开同分组。
- A 目标约 60%，边界最多允许多 1 人；C 取最接近约 5% 的末位完整同分组；其余为 B。
- 教师只可修改最终 A/B/C 等级，不能修改原始等级、累计分或排名。
- 可锁定和解锁结果。
- 最终 Excel 包含 30 项原始等级、五维累计分、五维排名、系统等级、教师确认等级、五维总分和总排名。

完整规则以 `docs/综合素质评价需求总结与完成度核对.md` 为准。

### 3.4 成绩管理

当前基础分析闭环已经完成：

- 支持语文、数学、英语、物理、化学、政治、历史、地理、生物九门学科。
- 每次考试只需导入实际参加考试的学科，不要求九科齐全。
- 支持从 `语文(120)` 等表头读取本次学科满分。
- 支持考试名称、日期、学期和年级。
- 支持按班级与学号匹配，缺少学号时按班级与姓名匹配。
- 同一考试重复导入时可更新已有成绩。
- 支持总分、班级排名、年级排名和同分并列。
- 支持学科平均分、最高分、最低分。
- 支持多次考试的学生排名趋势、班级趋势和学科趋势。
- 支持学生本次各科与班级均分对比，并以固定的正负 10% 判断优势或偏弱。

成绩模块仍是下一阶段最重要的功能完善对象，具体缺口见第 7 节。

### 3.5 请假与考勤

- 支持病假、事假、迟到、早退、缺勤和其他记录。
- 支持新增、编辑、删除和详情查看。
- 支持班级、类型、日期区间和关键词筛选。
- 支持统计记录总数及不同类型次数。
- 支持导出当前筛选结果为 Excel。

### 3.6 德育评价

- 支持集体活动、获奖荣誉、志愿服务、班级服务和社会实践记录。
- 支持奖项级别、主办方、日期、备注和教师手动积分。
- 支持学生累计记录和积分统计。
- 支持筛选、新增、编辑、删除和 Excel 导出。

### 3.7 课程表与日历

- 教学班可独立建立，不要求先建立学生班级。
- 教学班也可选择关联学生数据中心中的班级。
- 支持多个教学班并使用不同颜色区分。
- 支持学期开始、结束日期，并阻止学期日期重叠。
- 每日课时数量、顺序、名称和时间可自定义。
- 支持周一至周日课程表、单教学班编辑和全部教学班汇总查看。
- 支持班级日程和全局日程。
- 日历按班级颜色标记日程。

### 3.8 数据备份与恢复

- 支持完整 SQLite 快照 ZIP 备份。
- 支持选择学期创建归档包。
- 归档包包含说明、校验信息和完整数据库快照。
- 支持每天或每周自动备份，时间和目录可配置。
- 支持导入 ZIP 并一键恢复。
- 恢复前自动建立安全备份，并执行 ZIP、校验值和 SQLite 完整性检查。
- 当前自动备份只在软件保持运行时执行，尚未接入 Windows 任务计划程序。

### 3.9 工作台与可调布局

- 软件启动后默认进入“今日班级工作台”。
- 工作台汇总当天课程、日程、考勤和德育记录。
- 支持课程、日程、考勤和德育快捷入口。
- 正式程序中的侧栏、主要分栏和成绩详情分栏可拖动调节大小。
- 主要表格列可调宽、移动，并记忆分栏尺寸。
- 尚未实现任意仪表卡片的自由拖拽、排序和缩放。

## 4. 新版网页 UI 原型已完成内容

原型位置：`ui-prototype/`

当前预览地址：`http://localhost:4173/`，仅在本地预览服务运行时有效。

已完成：

- 参考示例的蓝色品牌侧栏、浅灰工作区、白色办公面板和语义色体系。
- 八个模块均可从侧栏切换。
- 顶部班级选择、日期选择和刷新操作。
- 工作台的指标卡、今日课表、今日待办、出勤圆环、趋势图和学生动态。
- 学生搜索与班级筛选。
- 综合素质的学期汇总、六学期档案、N/A 状态和教师核对视图。
- 成绩趋势、学科对比、可调进退步阈值和重点学生名单演示。
- 德育、考勤、周课表、课表编辑状态和备份流程演示。
- 1365×1015 与 1024×768 两种桌面尺寸检查。
- 浏览器控制台无错误；生产构建和站点结构测试通过。

重要边界：

- 原型使用 React、Vite、Lucide 和 Recharts。
- 原型数据均为模拟数据，不会读取或修改 `data/class_manager.db`。
- 原型中的导入、导出、备份等按钮主要用于验证交互反馈，不执行正式业务。
- 原型目前没有实现卡片自由拖拽或尺寸调整。
- 原型不会替代正式 PySide6 程序，确认后需要逐步迁移。

相关文件：

- `ui-prototype/src/App.jsx`
- `ui-prototype/src/styles.css`
- `ui-prototype/design-qa.md`
- `ui-prototype/artifacts/prototype-dashboard-final.png`

若下次预览服务未运行，可在 `ui-prototype/` 中执行 `pnpm run dev`，然后打开终端显示的本地地址。

## 5. 当前验证状态

2026-07-23 已重新运行正式桌面程序的全部冒烟测试：

| 测试范围 | 结果 |
| --- | --- |
| 学生中心与 Excel | 通过 |
| 综合素质基础评价 | 通过 |
| 综合素质六学期导入 | 通过 |
| 综合素质最终分档与锁定 | 通过 |
| 成绩管理 | 通过 |
| 请假与考勤 | 通过 |
| 德育评价 | 通过 |
| 日历日程 | 通过 |
| 独立教学班课程表 | 通过 |
| 课程表界面 | 通过 |
| 数据备份与恢复 | 通过 |
| 备份中心界面 | 通过 |

合计：**12 项通过，0 项失败**。

网页原型验证：

- Vite 生产构建通过。
- 站点 Worker 4 项测试通过。
- 八个侧栏入口、学生筛选、综合素质视图、成绩阈值、课表编辑和备份反馈已在浏览器中验证。
- `ui-prototype/design-qa.md` 最终结果为 `passed`。

## 6. 已取消或暂不开发

- 家访记录。
- 家长谈话记录。
- 综合素质扣分项目明细。
- 根据违纪自动扣减思想品德。
- 根据成绩、体育、艺术或社会实践自动生成每学期原始综合素质等级。
- 综合素质中的当地户籍、连续就读、均衡生资格和不符合原因字段。
- Q18、Q19 对应的资格筛选与特殊处理。
- 网络同步、账号登录和云端协作目前不在主线范围。

## 7. 已知缺口与待办

### P0：先由教师确认网页原型

- 确认蓝色侧栏的宽度、颜色和图标风格。
- 确认顶部班级、日期和操作区是否顺手。
- 确认工作台指标卡和面板的信息密度。
- 确认表格字号、行高和按钮大小。
- 确认成绩管理与综合素质页面是否是下一步优先迁移的两个模块。
- 决定是否真的需要卡片自由拖拽、排序和缩放，还是保留更稳定的可调分栏。

### P1：把已确认的视觉规范迁回正式 PySide6

- 先统一应用外壳：侧栏、顶部栏、页面背景、按钮、输入框、表格和状态标签。
- 再完成“今日班级工作台”的正式改版。
- 提炼可复用的 PySide6 组件，避免八个模块各写一套样式。
- 保留现有 QSplitter、表格列宽记忆和真实业务逻辑。
- 每完成一个模块运行对应冒烟测试和截图核对。

### P1：继续强化正式成绩分析系统

目前网页原型中的下列能力尚未进入正式 PySide6 成绩模块：

- 可输入或步进调整“进步/退步多少名”的阈值。
- 自动生成明显进步、明显退步和重点关注学生名单。
- 把当前表格趋势升级为更直观的班级、年级和学科图表。
- 为偏科学生生成更清晰的个体分析结论；当前只有固定正负 10% 的优势/偏弱判断。
- 支持成绩分析、学生个体报告和期末汇总的 Excel 导出。
- 支持 PDF 报告导出。
- 支持单独导入学校或教育部门提供的年级各科均分、四率等汇总表；当前年级均分由已导入的学生成绩计算。
- 兼容旧版 `.xls` 文件；当前正式导入主要支持 `.xlsx` 和 `.xlsm`。

### P2：统一其余模块视觉

建议顺序：

1. 综合素质评价。
2. 学生数据中心。
3. 课程表与日历。
4. 请假与考勤。
5. 德育评价。
6. 数据备份与恢复。

### P2：补齐跨模块导出

- 成绩 Excel 导出。
- 成绩和综合素质期末汇总报告。
- 全局 PDF 导出框架。
- 导出文件命名、页眉、班级、学期和生成日期统一规范。

### P3：工程整理

- 在确认无用后清理 `dist-python-updated/`、`dist-python-workbench/` 和旧构建缓存。
- 初始化有效 Git 仓库或建立明确的代码版本快照。
- 在最终发布前补充安装包图标、版本号、更新说明和新电脑验证。
- 仅在明确要求时重新打包 EXE。

## 8. 推荐的下一次开发顺序

### 第一步：收集本轮网页原型反馈

建议逐页记录三类意见：

- `保留`：视觉和交互已经合适。
- `修改`：颜色、尺寸、位置或信息内容需要调整。
- `删除`：实际工作中不需要的卡片、按钮或图表。

### 第二步：迁移正式程序的全局外壳

优先修改：

- `views/main_window.py`
- `resources/styles.qss`
- `utils/ui_layout.py`

本步只改公共导航、顶部区和通用控件，不同时重写全部业务页面。

### 第三步：改造正式工作台

以网页原型为视觉依据，将模拟数据替换为正式控制器数据，并保留现有快捷入口和可调分栏。

### 第四步：成绩模块功能和 UI 一起升级

建议本轮至少完成：

1. 可调进退步阈值。
2. 自动重点名单。
3. 班级/年级/学科趋势图。
4. 学生偏科结论。
5. 成绩 Excel 导出。

### 第五步：迁移综合素质和其余模块

综合素质业务规则已经稳定，迁移时只调整展示层，不改变已验证的赋分、排名、同分分档、教师调整和锁定规则。

### 第六步：整体验收和按需打包

- 运行全部 12 项冒烟测试。
- 进行真实 Excel 回归测试。
- 截图核对常见桌面分辨率。
- 创建完整 ZIP 数据备份。
- 用户明确要求后再生成新版 EXE。

## 9. 下次开始工作的快速检查表

- [ ] 先阅读本文件。
- [ ] 确认本次修改的是正式 PySide6 程序还是 `ui-prototype/`。
- [ ] 确认是否继续使用当前蓝色办公工作台方向。
- [ ] 确认本轮只做一个模块，不同时重写全部模块。
- [ ] 修改前运行对应冒烟测试并记录基线。
- [ ] 不改变综合素质已经确认的业务规则。
- [ ] 不把网页原型中的模拟功能写成“正式功能已完成”。
- [ ] 完成后再次运行相关测试和视觉检查。
- [ ] 未收到明确要求时不打包 EXE。

## 10. 下次可直接使用的任务开场语

```text
请先阅读项目根目录的《项目进度与下一步.md》，核对正式 PySide6 程序和 ui-prototype 网页原型的区别。继续按照已确认的蓝色办公工作台方向开发，不要修改综合素质既有业务规则，也不要打包 EXE。先处理第 7 节最高优先级的未完成任务，并在修改前后运行对应测试。
```

## 11. 关键文档与入口

- 项目简介：`README.md`
- 完整使用说明：`docs/使用说明.md`
- 综合素质最终规则：`docs/综合素质评价需求总结与完成度核对.md`
- 正式界面视觉核对：`design-qa.md`
- 网页原型视觉核对：`ui-prototype/design-qa.md`
- 正式程序入口：`main.py`
- 网页原型入口：`ui-prototype/src/App.jsx`
- 正式启动脚本：`启动班主任管理系统.bat`
- 按需打包脚本：`打包当前版本.bat`
