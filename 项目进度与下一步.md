# 班主任综合管理系统：项目进度与下一步

> 更新时间：2026-08-30
> 当前正式版本：`v1.0.1`（第四版）
> GitHub：`https://github.com/zheiseyummy/class-teacher-manager`
> 用途：下次继续开发前先阅读本文件，核对当前功能、既定规则和开发优先级。

## 1. 当前状态

项目已经统一为一套正式 PySide6 Windows 桌面程序。早期网页交互原型、阶段性截图、一次性 UI 审计报告和旧构建目录已于 2026-08-30 清理，不再作为开发依据。

| 项目 | 位置 | 说明 |
| --- | --- | --- |
| 程序入口 | `main.py` | 初始化数据库并启动 PySide6 主窗口 |
| 本地源码数据 | `data/class_manager.db` | 被 Git 忽略，不得上传 |
| 本地备份 | `backups/` | 被 Git 忽略，不得上传 |
| 当前 EXE | `dist-python/ClassTeacherManager/ClassTeacherManager.exe` | 本地构建成品，不进入源码仓库 |
| 当前便携包 | `dist-python/ClassTeacherManager-v1.0.1-win64.zip` | 新电脑完整解压后使用 |
| 正式源码 | `main / database / models / controllers / views / utils / resources` | 后续开发依据 |
| 自动化测试 | `tests/` | 13 项业务冒烟测试和可选视觉 QA |

`.venv/` 保留在本地以便继续开发，但不进入 Git。`build/`、`dist-python/`、截图和缓存均可重新生成。

## 2. 已完成模块

| 模块 | 状态 | 主要能力 |
| --- | --- | --- |
| 基础框架 | 已完成 | PySide6、SQLAlchemy、SQLite、统一样式、本地数据目录和启动诊断 |
| 教师信息 | 已完成 | 姓名、学校、任教学科、常用班级、默认学期、个人标记和首次设置 |
| 今日工作台 | 已完成 | 当日课程、日程、考勤、德育和近期考试波动学生 |
| 学生数据中心 | 已完成 | 多班级、学生和多个家长联系人、搜索、Excel 导入导出 |
| 综合素质评价 | 已完成 | 六学期五维、A/B/C/N/A、导入、排名、复核、锁定和最终档案 |
| 成绩管理 | 已完成基础闭环 | 九学科动态导入、考试留存、排名、学生/班级/学科趋势和偏科分析 |
| 请假与考勤 | 已完成 | 登记、筛选、统计、编辑、删除和 Excel 导出 |
| 德育评价 | 已完成 | 活动、奖项、社会实践、手动积分、筛选统计和 Excel 导出 |
| 课程表与日历 | 已完成 | 独立教学班、学期日期、自定义课时、多班颜色和班级/全局日程 |
| 数据备份与恢复 | 已完成 | 完整 ZIP、学期归档、定时备份、校验、恢复前备份和一键恢复 |
| EXE 发布 | 已完成 | 图标、Windows 版本信息、版本化 ZIP、SHA-256 和 Qt 启动兼容修复 |

## 3. 已确认的业务规则

### 3.1 产品边界

- Windows 本地运行，不依赖网络。
- 每位教师独立安装、独立数据库和独立备份。
- 不开发账号登录、多教师切换、云同步和复杂权限。
- 不开发家访、家长谈话等模块。
- PDF 导出仍是待开发项。
- 除非教师明确提出，不在每次修改后重新打包 EXE。

### 3.2 学生与班级

- 一个学生档案可以关联多个家长联系人。
- 支持多个行政班和多个独立教学班。
- 后续需要把学生身份与“某学期属于哪个班级”的就读关系分开。
- 转入、转出和换班不能破坏历史成绩或历史综合素质数据。
- 新学年需要支持整班升年级；初三结束后进入毕业归档，不自动创建高中班。

### 3.3 成绩

- 支持语文、数学、英语、物理、化学、政治、历史、地理、生物。
- 每次考试科目可以不同，未参加科目不按 0 分处理。
- 满分由考试导入时确认或从表头识别，不同年级和考试可以不同。
- 进退步重点参考班级排名和年级排名，默认比较同一学期内考试。
- 考试科目不一致时，总分不可直接等价比较，只比较共同科目并显示提示。
- 后续需加入可调名次阈值、明显进步/退步/持续波动/偏科关注名单。

### 3.4 综合素质

- 六学期：初一上、初一下、初二上、初二下、初三上、初三下。
- 五维度：思想品德、学业水平、身心健康、艺术素养、实践与创新。
- 原始等级只使用 A/B/C；缺失使用 N/A，不补分、不折算。
- 最终名单以九下实际名单为准，九下前转出学生保留历史但不进入最终评定。
- 五个维度分别按班级累计分排名，同分同名次，系统分档不拆分同分组。
- A 接近 60%，边界同分时最多允许多 1 人；C 取最接近约 5% 的末位完整同分组；其余为 B。
- 教师只能调整最终 A/B/C，不能修改六学期原始等级、累计分或排名。
- 锁定前由教师核对；锁定后需先解锁才能更正。
- Q18、Q19 的户籍、连续就读和均衡生资格需求已经取消。

### 3.5 备份

- 完整备份包含全部 SQLite 数据并写入校验信息。
- 学期归档保存完整数据库快照，并额外记录目标学期范围和统计。
- 恢复前必须自动创建当前数据的安全备份。
- 当前自动备份只在软件运行期间执行。
- 后续新学期或整班升年级向导执行前必须自动建立恢复点；备份失败时不能继续更新。

## 4. 当前验证状态

2026-08-30 最近一次正式回归结果：

- 13 项业务冒烟测试全部通过。
- `compileall`、`tabnanny` 和 `pip check` 通过。
- Windows 原生桌面与紧凑窗口视觉检查通过。
- `v1.0.1` EXE 文件版本、ZIP 内容和 SHA-256 校验通过。
- 构建目录 EXE 与 ZIP 重新解压后的 EXE 均在隔离数据目录成功启动。
- `v1.0.1` 修复了 PyInstaller 误收集 Poppler ICU DLL 导致的 `QtCore` WinError 127。

视觉 QA 脚本保留在 `tests/`，以后生成的图片统一写入 `build/visual-qa/`，不提交 GitHub。

## 5. 2026-08-30 仓库整理

已删除：

- 临时 `ui-prototype/` 网页原型及 Node 依赖；
- `.design/` 和 `docs/visual-qa-*.png` 阶段性截图；
- UI 审计、评分卡、交付报告和旧同类软件调研；
- 历史 `dist-python-updated/`、`dist-python-workbench/` 和 `build/`；
- `v0.9.0`、`v1.0.0` 旧 ZIP 及无版本旧包；
- 无引用的虚拟学生测试名单和多余 `.gitkeep`。
- 早期 `models/reserved.py` 中未使用的家访、谈心、违纪、班干部和评语预留模型；正式考勤记录已迁至 `models/attendance.py`。

已保留：

- 全部正式 Python 源码和业务测试；
- 当前本地数据库和备份目录；
- `.venv/` 开发环境；
- `v1.0.1` EXE 目录、ZIP 和 SHA-256；
- 正式 Excel 模板、图标、许可证和使用说明；
- 综合素质业务规则及下一阶段确认清单。

## 6. 下一阶段优先级

### P0：统一学期与学生就读历史

1. 建立统一学年和学期实体。
2. 将考试、综合素质、考勤、课程表和学生就读关系关联到统一学期。
3. 将学生身份与班级就读关系分离。
4. 迁移现有班级、课程表学期和考试学期数据，保证旧数据不丢失。

### P1：新学期与升年级向导

1. 同时选择一个或多个班级。
2. 同学年下学期沿用班级，不升年级。
3. 新学年自动建议初一 -> 初二、初二 -> 初三。
4. 支持新增、转入、移出和换班预览。
5. 可选复制上一学期课程表草稿。
6. 执行前自动完整备份，失败时整体回滚。

### P1：成绩导入向导

1. 导入前预览工作表、表头、学生匹配和异常记录。
2. 确认考试类型、学期、学科和各科满分。
3. 检查重复学号、未匹配学生、空值、缺考和超满分。
4. 保存字段映射和导入批次，考试锁定前支持整批撤销。
5. 后续补充 `.xls` 文件直接读取能力。

### P1：成绩分析与期末输出

1. 可调进退步名次阈值。
2. 自动生成明显进步、明显退步、持续波动和偏科关注名单。
3. 加强班级、年级、学生和单科趋势展示。
4. 支持教育局年级均分或四率表导入。
5. 完成期末成绩与综合评价 Excel/PDF 报告。

### P2：工程能力

- Windows 任务计划程序级自动备份；
- 安装程序、卸载入口和升级迁移说明；
- 更正式的数据迁移版本管理；
- 可选 PDF 导出和打印版式。

## 7. 下次开始前检查

1. 阅读本文件和根目录 `README.md`。
2. 执行 `git status --short --branch`，确认没有未知改动。
3. 确认 `config.py` 中版本号和目标任务是否需要发布变更。
4. 使用临时数据库运行相关测试，不读取或覆盖正式学生数据。
5. 修改前后运行对应冒烟测试；跨模块改动运行全部 13 项。
6. 视觉改动运行 `tests/visual_qa_capture.py`，输出只留在 `build/visual-qa/`。
7. 只有明确要求时才执行 `打包当前版本.bat`。
8. 删除、恢复、最终等级锁定和批量导出必须保留人工确认。

## 8. 下次可直接使用的开场语

```text
请先阅读项目根目录的《README.md》《项目进度与下一步.md》和 docs/下一阶段开发确认清单.md。当前正式版为 v1.0.1，网页原型和阶段性 UI 文件已经清理。下一步从“统一学期与学生就读历史”开始，先设计向后兼容的数据模型与迁移测试，再实现界面。不得修改综合素质既有业务规则；测试使用临时数据库；只有我明确要求时才重新打包 EXE。
```

## 9. 关键入口

- 完整项目说明：`README.md`
- 日常操作：`docs/使用说明.md`
- 下一阶段规则：`docs/下一阶段开发确认清单.md`
- 综合素质规则：`docs/综合素质评价需求总结与完成度核对.md`
- 版本记录：`CHANGELOG.md`
- 程序入口：`main.py`
- 打包入口：`打包当前版本.bat`
